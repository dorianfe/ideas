{% extends 'layout.html.twig' %}

{% block main %}
    <h2>{{ idea.getTitle }}</h2>
    <div class="description">{{ idea.getDescription }}</div>
    <div>category: {{ idea.category.getName()}}</div>
    <div>from user: {{ idea.getAuthor }}</div>
    <div>added: {{ idea.getDateCreated|date("d/m/Y") }}</div>
    


{% endblock %}

    {% block title %}{{ idea.getTitle }} | {{ parent() }}{% endblock %}